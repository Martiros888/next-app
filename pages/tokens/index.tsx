import React, { useState, useEffect, useMemo, useRef, memo, FC, } from 'react';

let Api:FC = () => {

    return (
        <>
            <section className=''>
                API
            </section>
        </>
    )
}

export default Api = memo(Api);